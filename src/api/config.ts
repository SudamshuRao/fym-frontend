/**
 * IMPORTANT: When running on a physical phone via Expo Go, "localhost"
 * refers to the PHONE itself, not your computer - so it will not reach
 * your backend running on your Mac. You need your computer's LAN IP
 * address instead.
 *
 * Find it with (on Mac, in a terminal): ipconfig getifaddr en0
 * Then replace the value below, e.g. "http://192.168.1.42:8000"
 *
 * If you're using an iOS Simulator or Android Emulator on the SAME
 * machine as the backend, "localhost" (iOS) or "10.0.2.2" (Android
 * emulator specifically) will work instead - but a real phone always
 * needs the LAN IP.
 */
export const API_BASE_URL = "http://192.168.1.202:8000"; // Replace with your backend's LAN IP address
